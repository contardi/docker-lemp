# /etc/msmtprc

defaults
auth           off
tls            off
logfile        /var/log/msmtp.log

# Mailpit account:
account        mailpit
host           mailpit
port           1025

# Use the mailpit account as the default:
account default : mailpit
